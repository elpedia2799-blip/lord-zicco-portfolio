import { prisma } from "@/lib/prisma";
export default async function ServicesAdmin(){const xs=await prisma.service.findMany({orderBy:{sortOrder:"asc"}});return <div className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold">Services</h1><p className="mt-1 text-white/40">Manage the services shown on your website.</p><div className="mt-7 space-y-3">{xs.map(x=><div className="rounded-2xl border border-white/10 bg-[#0a0a0a] p-5" key={x.id}><div className="flex items-center justify-between"><div><b>{x.name}</b><p className="mt-1 text-sm text-white/40">{x.description}</p></div><span className="text-xs text-white/30">{x.enabled?"Enabled":"Disabled"}</span></div></div>)}</div></div>}
