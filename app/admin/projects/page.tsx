import { prisma } from "@/lib/prisma";
export default async function ProjectsAdmin(){const ps=await prisma.project.findMany({include:{category:true},orderBy:{createdAt:"desc"}});return <div className="mx-auto max-w-7xl"><div className="flex items-end justify-between"><div><h1 className="text-3xl font-bold">Projects</h1><p className="mt-1 text-white/40">Add, publish and organize portfolio work.</p></div><button className="btn btn-red">+ Add Project</button></div><div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{ps.map(p=><div className="overflow-hidden rounded-2xl border border-white/10 bg-[#0a0a0a]" key={p.id}><img src={p.imageUrl} alt={p.title} className="aspect-[4/3] w-full object-cover"/><div className="p-5"><div className="text-xs text-red">{p.category.name}</div><h2 className="mt-1 font-bold">{p.title}</h2><p className="mt-3 text-xs text-white/35">{p.published?"Published":"Draft"} · {p.views} views</p></div></div>)}</div></div>}
